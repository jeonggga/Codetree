# 사용자로부터 정수 N을 입력받음
n = int(input())

# 1부터 차례대로 더한 값을 저장할 변수 (처음엔 0부터 시작)
num = 0

# 1부터 100까지 1씩 증가하면서 반복
for x in range(1, 101):
    num += x          # 현재 숫자 x를 누적 합 num에 더함
    if num >= n:      # 누적 합이 N 이상이 되는 순간
        break         # 반복문을 즉시 종료함
print(x)              # 그 순간에 더해진 숫자 x를 출력
